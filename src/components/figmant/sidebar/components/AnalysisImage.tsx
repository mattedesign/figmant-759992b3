
import React from 'react';

interface AnalysisImageProps {
  analysis: any;
  title: string;
}

export const AnalysisImage: React.FC<AnalysisImageProps> = ({ analysis, title }) => {
  // Component now returns null to remove the image entirely
  return null;
};
